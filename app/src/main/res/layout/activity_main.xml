<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:fitsSystemWindows="true"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_height="match_parent">
        <androidx.appcompat.widget.Toolbar
            app:menu="@menu/menu_toolbar"
            android:background="@color/white"
            android:layout_gravity="center"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/toobar">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="horizontal">
            <EditText
                android:id="@+id/edt_search"
                android:layout_width="225dp"
                android:hint="Tìm kiếm..."
                android:gravity="center|start"
                android:paddingLeft="20dp"
                android:textSize="16dp"
                android:inputType="text"
                android:maxLines="1"
                android:textColorHint="@color/gray"
                android:textColor="@color/black"
                android:background="@drawable/border_edt_search"
                android:layout_height="40dp"/>
            <ImageView
                android:id="@+id/btn_search"
                android:layout_width="50dp"
                android:layout_marginLeft="10dp"
                android:layout_marginRight="10dp"
                android:background="@drawable/borber_btn_search"
                android:backgroundTint="@color/blueIndex"
                android:padding="5dp"
                android:layout_height="40dp"
                android:src="@drawable/ic_search"
                />
        </LinearLayout>
        </androidx.appcompat.widget.Toolbar>
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/Fragment_Container"
            android:layout_width="match_parent"
            android:background="@color/white"
            android:layout_weight="1"
            android:layout_height="match_parent"/>
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/nav_bottom"
            app:itemIconTint="@color/selector_color_nav_bottom"
            app:itemTextColor="@color/selector_color_nav_bottom"
            app:menu="@menu/menu_bottom_nav"
            android:background="@color/select_color_nav_bottom_backgrouf"
            android:layout_width="match_parent"
            app:itemBackground="@color/select_color_nav_bottom_backgrouf"
            android:layout_height="wrap_content"/>
    </LinearLayout>
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav"
        android:fitsSystemWindows="true"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:menu="@menu/menu_nav"
        app:itemTextColor="@color/color_text_title_groud"
        app:itemTextAppearance="@color/color_text_title_groud"
        app:itemIconTint="@color/black"
        android:background="@color/white"
        app:headerLayout="@layout/header_nav"
        android:layout_gravity="start"/>
</androidx.drawerlayout.widget.DrawerLayout>